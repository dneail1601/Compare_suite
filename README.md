# Compare_suite
Lightweight browser based compare suite for text, hex, json and a multi grep

Should run in every browser without much dependencies. Lightweight so it should run smoothly on an SBC.

Multi grep was not tested, since I do not need it.
